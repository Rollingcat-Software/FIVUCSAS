# References Audit — FIVUCSAS Thesis

Generated by sweeping every `[CITE:key]` marker across `build/chapters/*.md` and
`build/facts/*.md`, then reconciling against `build/bibliography.md`.

- **54** distinct citation keys are used across the chapters and fact packs.
- **53** already resolved in the bibliography.
- **1** missing key (`jjwt`) was added in correct Guide (web/library) format.
- **0** citations support a claim that the fact packs contradict. (One borderline
  case — a deliberate *disavowal* of the unverified "100% accuracy" poster claim —
  is flagged below as correct-by-design, not a problem.)

---

## (a) Every cited key — resolution status

All keys below resolve to an entry in `build/bibliography.md`.

| Key | Resolves | Key | Resolves |
|---|:---:|---|:---:|
| bazarevsky2019-blazeface | ✓ | kvkk6698 | ✓ |
| bcrypt | ✓ | mediapipe | ✓ |
| bucket4j | ✓ | minifasnet | ✓ |
| challapalli2024-mtcnn | ✓ | newman2021-microservices | ✓ |
| chen2018-mobilefacenets | ✓ | oauth2-rfc6749 | ✓ |
| cockburn-hexagonal | ✓ | oidc-core | ✓ |
| deepface-lib | ✓ | opencv | ✓ |
| deng2019-arcface | ✓ | owasp-top10 | ✓ |
| docker | ✓ | parkhi2015-vggface | ✓ |
| dockercompose | ✓ | pgvector | ✓ |
| evans2003-ddd | ✓ | pkce-rfc7636 | ✓ |
| faiss | ✓ | playwright | ✓ |
| fastapi | ✓ | postgresql | ✓ |
| flyway | ✓ | react | ✓ |
| fowler2014-microservices | ✓ | redis | ✓ |
| gdpr | ✓ | richardson2018-microservices | ✓ |
| icao9303 | ✓ | schroff2015-facenet | ✓ |
| iso30107-3 | ✓ | serengil2020-lightface | ✓ |
| itrc2023 | ✓ | soukupova2016-ear | ✓ |
| jjwt | ✓ (added — see (b)) | springboot | ✓ |
| jwt-rfc7519 | ✓ | taigman2014-deepface | ✓ |
| k6 | ✓ | testcontainers | ✓ |
| kim2022-adaface | ✓ | traefik | ✓ |
| kmp | ✓ | verizon2024-dbir | ✓ |
| koch2015-siamese | ✓ | webauthn | ✓ |
| kottakota2024-antispoof | ✓ | yolov8 | ✓ |
| | | zhang2016-mtcnn | ✓ |

**Bibliography keys defined but not cited anywhere** (left in place — harmless, may be
used by future revisions): `beltran2023-privacy`, `linddun`, `nginx`. None are removed.

---

## (b) Keys added to the bibliography

| Key | Reason | Entry added |
|---|---|---|
| `jjwt` | Cited in `ch4.md:355` and `facts/identity_core.md:128` as the Java JWT library backing `JwtService` (jjwt 0.12.6, `io.jsonwebtoken`). Missing from the bibliography. | `Hazlewood, L. and JJWT Contributors, *JJWT — Java JWT: JSON Web Token for Java and Android*, https://github.com/jwtk/jjwt, Date accessed: June 2026.` |

The entry follows the Guide's web/software-library format (same shape as
`deepface-lib`, `pgvector`, `bucket4j`, `testcontainers`). The author/source is the
real upstream project: JJWT by Les Hazlewood and contributors, repository
`github.com/jwtk/jjwt` (verified upstream — no authors invented).

---

## (c) Citations whose claim is NOT backed by the fact packs (flagged, not deleted)

**None require correction.** Every technical citation was spot-checked against the
fact packs and resolved cleanly. Representative checks:

- `pgvector` + IVFFlat / `lists = 100` / `vector_cosine_ops` (ch3, ch4, ch5) —
  backed verbatim by `facts/biometric.md` §8 (lines 276–303); the fact pack even
  prescribes these exact keys for the ANN search surface (line 375).
- `VERIFICATION_THRESHOLD = 0.4` prod, aged relaxation `0.55`, cosine-distance
  semantics (ch3) — backed by `facts/biometric.md:293–297` (code default 0.45, prod
  override 0.4; aged-threshold env vars).
- `iso30107-3` + CASIA-FASD micro-benchmark (ch5:22, ch7:163) — the `CasiaFasdMicroBench`
  harness is real (`facts/biometric.md:234`); the chapters correctly describe it as a
  micro-benchmark in the test suite, **not** as an audited PAD result.
- `bcrypt` work factor 12, `jwt-rfc7519` RS256 + iss/aud, `bucket4j` token buckets,
  `webauthn` (Yubico server lib + origin allowlist), `redis` multi-purpose use —
  all backed by `facts/identity_core.md`.
- `kmp` / Flutter abandonment, `traefik` vs the ADD's NGINX gateway, RLS-inert
  honesty note — all consistent with the fact packs and CLAUDE.md ground truth.

**One borderline item — correct by design, no action:** `ch5.md:22` and `ch7.md:163`
cite `iso30107-3` while *explicitly refusing* to repeat the unverified "100% accuracy"
fuser claim. This is the accuracy rule working as intended (the poster/learned-fuser
number is flagged UNVERIFIED in `_SHARED_CONTEXT.md` and the fact packs), so the
citation supports an honest disavowal rather than an unbacked metric. Retained.
