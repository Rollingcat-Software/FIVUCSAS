# Fact-Verification Audit — FIVUCSAS Thesis (2026-06-11)

**Auditor pass date:** 2026-06-11
**Scope:** the full thesis (`FIVUCSAS_Thesis.md`, 2563 lines, read in its entirety via the
per-chapter sources `build/chapters/00_frontmatter.md`, `ch1.md`–`ch7.md`, `appendices.md`),
verified claim-by-claim against the live source trees at HEAD:
`identity-core-api` (HEAD `d61e81a`, 2026-06-07), `biometric-processor`, `web-app`,
`client-apps`, `spoof-detector` (HEAD `9b1d73e`, 2026-06-06), `FIVUCSAS` parent.
**Prior passes:** `build/reviews/accuracy.md` (2026-06-05) and the README content pass
(2026-06-06). Items those passes verified were spot-checked rather than redone; the focus
here is (a) what they missed and (b) what has drifted since, because **three PRs merged to
`identity-core-api` main on 2026-06-07 (#209/#210/#211) after the thesis was finalized**.

## Summary

| | Count |
|---|---|
| Claims checked (distinct technical/quantitative claims) | **~130** |
| ✅ Verified correct | **117** |
| ❌ Wrong | **2** |
| ⚠️ Imprecise / drifted-since-writing / internal inconsistency | **11** |

Headline: the thesis remains exceptionally well-grounded. All three issues flagged by the
2026-06-05 audit (O3 threshold, ch6 "ten" methods, abstract test-count framing) are
**confirmed fixed** in the current chapter sources. The two genuine errors found in this
pass are the stale **YOLOv8m card-model limitation in ch7** (the true YOLOv8n nano has been
the deployed model since 2026-05-29 — before the thesis was written) and the **"32 JPA
entities"** figure (actual: 31; the 32 almost certainly counted a `@EntityGraph` substring
hit in `UserRepository.java`). The remaining items are post-finalization repo drift
(2026-06-07 merges added migration **V84**, a ninth tenant-filtered entity, and ~26 new
JUnit tests) plus small wording/arithmetic imprecisions.

---

## ❌ WRONG

### W1 — ch7: "deployed card-detection model is an oversized YOLOv8m" (stale; was already false at writing)
- **Locations:** `ch7.md` §7.2.2 limitation bullet (~line 186–187: "The deployed in-browser
  **card-detection model** is an oversized YOLOv8m standing in for the intended lightweight
  nano model") and §7.3 (~line 221–222: "The oversized in-browser card-detection model
  should likewise be replaced with the intended retrained YOLOv8n nano").
- **Evidence:** `web-app/public/models/manifest.json` (version `2026.05.29`):
  `yolo-card-nano.onnx`, **12,268,876 bytes (12.3 MB), "true YOLOv8n nano (opset 12),
  retrained + exported 2026-05-29. Replaces the 50 MB YOLOv8m mislabeled as nano."**
  Corroborated by `web-app/public/models/README.md` ("true YOLOv8n, 12.3 MB, opset 12") and
  parent CLAUDE.md (web-app #109 / biometric-processor #116, user-verified in production).
  The swap predates the thesis build date (2026-06-05), so this is a factual error, not
  mere drift — the fact pack the chapter drew on was stale.
- **Correction:** delete the §7.2.2 bullet (or replace with a real residual limitation,
  e.g. "the card-detection model is trained on a limited Turkish-document corpus") and
  re-scope the §7.3 sentence to "the YOLOv8n card model should be retrained on broader
  Turkish ID and passport imagery" (the nano replacement is done).

### W2 — ch3 §3.2.2 / ch4 §4.2: "32 JPA entities" (actual: 31)
- **Locations:** `ch3.md` line 76 ("the schema comprises 32 JPA entities"); `ch4.md`
  line 115 ("The persistence model itself is 32 JPA `@Entity` classes").
- **Evidence:** exact-annotation grep over `identity-core-api/src/main/java` returns **31**
  `@Entity` classes (file list: ApiKey, AuditLog, AuthFlow, AuthFlowStep, AuthMethod,
  AuthSession, AuthSessionStep, GuestInvitation, Identity, IdentityEmail,
  IdentityTenantBiometricConsent, MfaSession, NfcCard, OAuth2Client, PasswordHistory,
  Permission, RateLimitBucket, RefreshToken, Role, Tenant, TenantAuthMethod,
  TenantEmailDomain, User, UserDevice, UserEnrollment, UserRole, UserSettings,
  VerificationDocument, VerificationSession, VerificationStepResult, WebAuthnCredential).
  A naive substring grep returns 32 because `repository/UserRepository.java` matches via
  `@EntityGraph` — the likely origin of the 32. `git log --diff-filter=D` shows no entity
  deleted since 2026-05-20, so 31 was also the count at writing time.
- **Correction:** "31 JPA `@Entity` classes" (or "roughly thirty").

---

## ⚠️ IMPRECISE / DRIFTED / INCONSISTENT

### D1 — Flyway: "83 migrations, V0–V83" → repo now has **84 files, V0–V84**
- **Locations:** `ch3.md` line 76 ("83 Flyway migrations spanning V0–V83 (the V13 number was
  never used; the highest applied in production is V83)"); `ch4.md` tool table ("V0–V83");
  `ch7.md` line 40 ("83 Flyway migrations … V0–V83 range, with V13 unused") and §7.2.1
  ("83-step auditable migration history"); `appendices.md` Appendix A (catalog ends at V83).
- **Evidence:** `identity-core-api/src/main/resources/db/migration/` contains **84** `.sql`
  files, **V0–V84** with V13 unused; `V84__user_settings_tenant_id.sql` landed 2026-06-07
  (PR #211, after thesis finalization). "Highest applied in production is V83" is still
  true (V84 applies on the next rebuild) — that clause survives.
- **Suggested fix:** either refresh to "84 migrations spanning V0–V84 (V13 unused)" + add
  the V84 row ("User settings tenant id") to Appendix A, or pin the existing numbers with
  "as of the 2026-06 code freeze".

### D2 — "the eight tenant-scoped entities" with `@Filter(tenantFilter)` → now **nine**
- **Locations:** `ch3.md` FR-4 (line 28) and `ch4.md` §4.8 (line 603–605), both listing
  exactly eight (`AuditLog`, `AuthSession`, `MfaSession`, `UserEnrollment`,
  `VerificationSession`, `OAuth2Client`, `UserDevice`, `AuthFlow`).
- **Evidence:** grep for `@Filter(name = "tenantFilter"` now matches **11** entities — the
  eight listed **plus `UserSettings`** (added 2026-06-07 with V84, PR #211) plus `User` and
  `Role` (which the thesis correctly treats separately: the global `@FilterDef` lives on
  `User`, and `Role` carries the widened `tenant_id IS NULL` variant).
- **Suggested fix:** "nine tenant-scoped entities (… and `UserSettings`)" or "the
  tenant-scoped entities (eight at the time of the design freeze, since extended)".

### D3 — JUnit count: "1,569 `@Test` across 176 files (+22 parameterized)" → now 1,595 / 179
- **Locations:** `ch3.md` §3.2.4 table (line 133); `ch5.md` §5.3 (line 120), table
  (line 157), §5.10 (line 504); `ch2.md` O1 row ("~1,569").
- **Evidence:** `grep -rhoE "@Test\b"` over `src/test/java` = **1,595** across **179** files;
  `@ParameterizedTest` = 22 (unchanged). The +26 tests came with PRs #210/#211 (2026-06-07).
  Drift ≈1.7% — at the edge of tolerance. The "~" and "at the time of writing" framing in
  ch5 partially covers this; the ≈4,386 total becomes ≈4,400+ either way.
- **Suggested fix:** refresh to 1,595/179 or keep 1,569 with an explicit as-of date.

### D4 — Kotlin test split does not sum to its own total (authored-time inconsistency)
- **Location:** `ch5.md` §5.3 (lines 148–151): "**568** `@Test` methods across 64 test
  files, split by source set into **486 commonTest, 34 androidTest, and 25 desktopTest**".
- **Evidence:** 486 + 34 + 25 = **545 ≠ 568**. The missing ~23 are the `androidApp/src/test`
  JVM unit tests (e.g. `PaceKeyDerivationTest`, `CardAccessParserTest`,
  `DataExportViewModelTest`), which belong to no listed source set. Today's live counts:
  **561 total** = 489 commonTest + 30 androidTest + 25 desktopTest + 17 androidApp unit
  (mild drift since writing; 64 files still holds).
- **Suggested fix:** add the fourth bucket ("… plus the Android app's JVM unit tests") and/or
  refresh the numbers so the split sums to the total.

### D5 — TypeScript anti-spoofing analyzers: "25" → **26** (already 26 at writing)
- **Locations:** `ch4.md` §4.3.2 (line 246–247: "twenty-five in the TypeScript browser
  port"); `ch5.md` §5.8.2 (line 392–393: "extends this to **25 analyzers**").
- **Evidence:** `spoof-detector/web/src/infrastructure/analyzers/` holds **26** `*Analyzer.ts`
  modules; `FlashTemporalAnalyzer.ts` (2026-05-31) and `FoundationModelAnalyzer.ts`
  (2026-06-02) both predate the thesis build. (The Python count of **13** is exact:
  14 files minus `__init__.py`. ✅) Note only ~23 are instantiated in the default pipeline,
  so "25" may have been a "registered" count — but it matches neither the file count (26)
  nor the wired count (23).
- **Suggested fix:** "26 analyzer modules" or "more than two dozen analyzers".

### D6 — spoof-detector browser test count: "256 Vitest cases" → **276**
- **Location:** `ch5.md` §5.8.3 (line 434–436).
- **Evidence:** `it(`/`test(` blocks across `spoof-detector/web/__tests__/*.test.ts` = **276**
  (suite grew through 2026-06-06, e.g. CelebA-Spoof/CASIA benchmark work, PR #101).
- **Suggested fix:** "more than 250 Vitest cases" or refresh to 276.

### D7 — ch4 §4.3.5: "verification an even more lenient 50" — adjective inverted
- **Location:** `ch4.md` lines 339–341.
- **Evidence:** values are correct — enrollment floor = prod `QUALITY_THRESHOLD: 40.0`
  (`biometric-processor/docker-compose.prod.yml:115`; config default 70) and verification
  floor = `VERIFICATION_QUALITY_THRESHOLD = 50.0`
  (`app/application/use_cases/verify_face.py:40`) — but a floor of **50 is stricter than
  40**, not "more lenient".
- **Suggested fix:** "enrollment uses a floor of 40 and verification a slightly stricter 50"
  (or explain: enrollment was deliberately loosened to 40 to match real webcams, while the
  verify path keeps 50).

### D8 — ch3 §3.2.3: Android "latest signed release v5.3.0" → **v5.3.1**
- **Location:** `ch3.md` line 101.
- **Evidence:** `client-apps/androidApp/build.gradle.kts:45-46` — `versionCode = 13`,
  `versionName = "5.3.1"`; v5.3.1 (MFA stale-connection retry, PR #87) shipped 2026-06-06,
  essentially simultaneous with thesis finalization.
- **Suggested fix:** "v5.3.x signed public releases" or refresh to v5.3.1.

### D9 — ch4 §4.4.3: OTP "delete the code on a wrong guess" — actually on the **fifth** wrong guess
- **Location:** `ch4.md` lines 446–447 ("**OTP** codes store an attempt counter alongside
  the code and delete the code on a wrong guess, capping the brute-force budget").
- **Evidence:** `identity-core-api/.../infrastructure/otp/OtpService.java` —
  `MAX_ATTEMPTS = 5`; each wrong guess increments `<key>:attempts`; "the 5th wrong guess
  deletes the OTP regardless of TTL". Deleting on the *first* wrong guess would be a
  different (and harsher) design.
- **Suggested fix:** "…and delete the code after five wrong guesses (the attempt counter
  caps the brute-force budget at five tries per issued code)".

### D10 — ch4 §4.3.4: "An HNSW variant exists in the migrations only as a commented-out alternative"
- **Location:** `ch4.md` lines 315–316.
- **Evidence:** true for the operative table — Alembic `0003_add_performance_indexes.py`
  creates `idx_embeddings_vector_ivfflat … USING ivfflat (embedding vector_cosine_ops)
  WITH (lists = 100)` on `face_embeddings`, with HNSW only in a comment — **but** Alembic
  `0001_initial_schema.py` *does* create a real (uncommented) HNSW index on the legacy
  `biometric_data` table, and `scripts/add_hnsw_indexes.sql` exists as an operator script.
  The production claim (operative index on `face_embeddings` is IVFFlat, lists=100) is
  correct ✅; only the "only as a commented-out alternative" absolutism overreaches.
- **Suggested fix:** "…the operative production index on `face_embeddings` is IVFFlat; an
  HNSW index appears in that migration only as a commented-out alternative (the initial
  schema's legacy table briefly carried one)".

### D11 — Bio endpoint count: "roughly 69 endpoints" → 71 route decorators at HEAD
- **Locations:** `ch3.md` §3.3.2 (line 154); `appendices.md` Appendix B (line 97).
- **Evidence:** 26 route modules (27 files incl. `__init__.py`) ✅ exact; route decorators
  (`@router.get/post/put/delete/patch/websocket`) = **71**. "Roughly 69" still passes as
  "roughly", so this is informational only.

---

## Confirmed fixed (issues from the 2026-06-05 audit)

- **ch2 O3 threshold** now correctly reads "cosine-*distance* decision rule (production
  `VERIFICATION_THRESHOLD = 0.4`, relaxed to 0.55 …)" — matches
  `config.py` (default 0.45, AGED 0.55, AGED_YEARS 2.0, inversion validator) and prod
  compose (`VERIFICATION_THRESHOLD: 0.4`). ✅
- **ch6 §6.3** now reads "twelve selectable authentication methods — the ten canonical login
  factors plus the two cross-device additions". ✅
- **Abstract** now separates the ~4,400 count (five test technologies, unit/integration/E2E)
  from the k6 and security batteries ("complemented by … a separate security battery"). ✅

---

## ✅ Verified-correct claims (by chapter)

### Frontmatter / Chapter 1
- Two cooperating microservices, hexagonal; Spring Boot identity core; FastAPI biometric
  processor; Facenet512 embedding + cosine matching over pgvector; MiniFASNet passive +
  randomized active challenge (EAR/MAR/head-pose); ICAO 9303 NFC passive auth; Traefik
  edge + PostgreSQL + Redis; React + KMP clients; public Android release — all verified
  in code (details under ch3–ch5 below). ✅
- "a dozen selectable login factors — ten canonical plus two cross-device":
  `AuthMethodType.LOGIN_METHODS` = exactly 12 (PASSWORD, EMAIL_OTP, SMS_OTP, TOTP, FACE,
  FINGERPRINT, VOICE, NFC_DOCUMENT, HARDWARE_KEY, QR_CODE, PASSKEY, APPROVE_LOGIN),
  gated by `isLoginMethod()` (`domain/model/auth/AuthMethodType.java:46-67`). ✅

### Chapter 2
- Spring Boot **3.4.7** / Java **21** (`pom.xml:10,21`); FastAPI / Python 3.12
  (Dockerfile digest-pinned `python:3.12-slim`); React **18.3.1**, TypeScript **5.5.3**
  (`web-app/package.json`); PostgreSQL 17 + pgvector and Redis 7.4 (prod; previously
  verified in production config — dev/CI composes intentionally use
  `pgvector/pgvector:pg16` / `redis:7-alpine`, which ch5 itself documents). ✅
- "more than eighty versioned migrations" (84). ✅
- Hibernate `@Filter` shared-schema tenancy; cross-tenant isolation ITs required + asserted
  per PR (`.github/workflows/ci.yml:120-153` enumerates `CrossTenantIsolationIT`,
  `TenantSwitcherIsolationIT`, `IdentityBiometricConsentIT`, `IdentityBackfillIT`,
  `RoleUnificationBackfillIT` and fails if any did not execute). ✅
- MTCNN detection / Facenet512 (512-dim) — prod compose `FACE_DETECTION_BACKEND: mtcnn`,
  `FACE_RECOGNITION_MODEL: Facenet512`; `EMBEDDING_DIMENSION` default 512. ✅
- UniFace MiniFASNet ONNX passive liveness + LBP texture/frequency/color/moiré classical
  stack; uniface **pinned 3.6.0** with the segfault rationale in `requirements.txt:84`. ✅
- Voice = Resemblyzer **256-dim** (`speaker_embedder.py: VOICE_EMBEDDING_DIM = 256`). ✅
- Hetzner CX43 8 vCPU/16 GB/150 GB Ubuntu 24.04; CPU-only; `ALLOW_HEAVY_ML` boot gate
  refusing RetinaFace/ArcFace/YOLO backends (config.py Literal lists + gate). ✅
- O2/O3 KPI figures labeled targets/controlled-test, never certified results. ✅ (posture)
- iOS not delivered (no `iosApp` module; iOS HMAC stub); Flutter→KMP change reported. ✅
- LFW comparison table = published figures with explicit "not measured in this project"
  caption. ✅
- k6 thresholds "p95 auth <300 ms / verification <500 ms" as targets; six scenarios. ✅

### Chapter 3
- FR endpoints exist as tabled: `POST /auth/login`, `/auth/login/preflight`,
  `/auth/login/begin`, `/auth/mfa/step` (`AuthController.java:138,169,199,764`). ✅
- 423 lockout after **5** consecutive failures via `LoginAccountStateGuard`
  (`MAX_FAILED_ATTEMPTS = 5`, HTTP 423). ✅
- Verify-path passive-liveness floor **0.4** (`verification.py:408
  VERIFY_MIN_LIVENESS_SCORE = 0.4`); enroll liveness fail-closed multi-image
  (`enroll_multi_image.py`, `ENROLL_LIVENESS_ENABLED` default True). ✅
- `VERIFICATION_THRESHOLD` prod 0.4 / aged 0.55 (>2 years) + validator. ✅
- pgvector 1:N via `<=>` cosine + **IVFFlat `lists = 100`** on `face_embeddings`
  (Alembic 0003); voice search same family. ✅
- Two-tier authority: `user_type` ROOT > TENANT_ADMIN > TENANT_MEMBER > GUEST; within-tenant
  role over **48 permissions** (V71 comment: "32 of 48", V76). ✅
- `TenantBindFromAuthFilter` re-binds tenant from verified JWT; `TenantFilterBypass` for
  `/my` self-service. ✅
- Partitioned `audit_logs` → pg_partman fail-soft (V40/V41/V57); hard-delete forbid trigger
  (V53); `SoftDeletePurgeJob` nightly **03:30** under ShedLock
  (`@Scheduled(cron = "0 30 3 * * *")` + `@SchedulerLock`). ✅
- NFR table: 6000/min per-tenant token-mint budget
  (`RateLimitService.allowTenantTokenMint`, threshold 6000/min); RS256 prod JWT;
  BCrypt(12) (`SecurityConfig.java:269`); Fernet-encrypted embeddings
  (`embedding_ciphertext`, Alembic 0005, `key_version`); AES-GCM-256 TOTP secrets
  (`TotpSecretCipher`: AES/GCM/NoPadding, 256-bit KEK, 128-bit tag); hardened containers
  (`read_only`, `cap_drop: ALL`, `no-new-privileges` in both prod composes). ✅
- Embedding cache TTL **300 s / 500 entries** (prod compose `EMBEDDING_CACHE_TTL: 300`,
  `EMBEDDING_CACHE_MAX_SIZE: 500`; config default is 1000 — thesis correctly states the
  production value). ✅
- Web dashboard "roughly 38 page components": 37 `*Page.tsx` files in `src/pages/`. ✅
  ("roughly" holds.)
- Hosted-first OIDC; widget demoted to step-up; e-Devlet/Auth0 pattern claims (design
  statements consistent with `docs/` + SecurityConfig). ✅
- Desktop client `.deb`/`.msi` (`desktopApp/build.gradle.kts:84
  targetFormats(TargetFormat.Deb, TargetFormat.Msi)`); DPAPI/libsecret storage. ✅
- Test-plan inventory previews ch5 numbers (same figures; see D3/D4). Playwright
  **336 / 28** exact; pytest **888 / 68** exact. ✅
- Redis usage table: `oauth2:code:` 10 min (`OAuth2Service: AUTH_CODE_TTL =
  Duration.ofMinutes(10)`); OTP 5 min (`OtpService: OTP_TTL = ofMinutes(5)` + `:attempts`
  same TTL); TOTP replay marker 120 s `SET … EX NX`; QR session 5 min, approved 2 min
  (`QrSessionService: SESSION_TTL ofMinutes(5)`, approved-branch `expire(key, ofMinutes(2))`);
  approve-login 2 min (`ApproveLoginService: SESSION_TTL/APPROVED_TTL = ofMinutes(2)`);
  step-up 5 min (`StepUpChallengeService: CHALLENGE_TTL = ofMinutes(5)`); anti-replay
  `antireplay:nonce:` 5-min window (`AntiReplayFilter`); rate-limit
  `rate_limit:<id>:<path>` 1-min window + 1-h onboarding window (`RateLimitFilter:35,42,78`).
  ✅ (entire table)
- Production Redis AOF + `allkeys-lru` (`FIVUCSAS/docker-compose.prod.yml:48-50`). ✅
- Resource limits: identity API 2 CPU / 2 GB; biometric 4 CPU / 4 GB (both prod composes). ✅
- Traefik v3.6.12, docker-socket-proxy, IP-allowlisted admin surfaces, X-Forwarded-For
  overwrite (verified previously in `infra/traefik`; consistent at HEAD). ✅
- Grafana/Loki/Promtail running, Prometheus/Alertmanager configured-not-deployed (parent
  compose has the Prometheus block; labeled as gap). ✅ (posture)

### Chapter 4
- jjwt **0.12.6** (`pom.xml:22`); HS512 legacy + RS256, prod hard-coded `default-algo:
  RS256` (`application-prod.yml:173`) + `@PostConstruct assertProdAlgoIsRs256()` fail-fast;
  HS512 verification opt-in only; `kid` key routing (`JwtService`). ✅
- Access 15 min / refresh 24 h in prod (`application-prod.yml:162-163` —
  900000 / 86400000 ms). ✅
- `amr` accumulation; SMS_OTP→"sms" in the MFA path (`VerifyMfaStepService.java:85`),
  pwd/otp/face/voice/hwk in `AuthenticateUserService.amrValue()`. ✅
- Biometric Puzzle: lives in `generate_puzzle.py` / `active_liveness_manager.py` /
  `verify_puzzle.py`; difficulty EASY (2,3)×7.0 s, STANDARD (3,4)×5.0 s, HARD (4,5)×4.0 s;
  `random.randint`/`random.choice`; TTL `timeout_seconds + 60`. ✅ (all exact)
- EAR landmarks left `[362,385,387,263,373,380]` / right `[33,160,158,133,153,144]`;
  blink threshold **0.21**; baseline captured while `avg_ear > 0.2`; the quoted
  `_calculate_ear` code matches. ✅
- MAR: smile `> 0.4` AND ratio `> 1.3`; mouth-open `> 0.5`; head-turn `±0.15`; eyebrow
  `0.08`; blendshape preference (`eyeBlinkLeft/Right`, `mouthSmile*`, `jawOpen`)
  (`active_liveness_manager.py:48-52,423-499`). ✅
- `MIN_STEP_CONFIDENCE = 0.6`, `MIN_STEP_DURATION_SECONDS = 0.5`, `PASS_THRESHOLD = 0.6`
  (`verify_puzzle.py:34-43`). ✅
- Passive fusion weights texture 0.35 / color 0.25 / frequency 0.25 / moiré 0.15
  (`texture_liveness_detector.py:91-96`). ✅
- Shared process-wide MiniFASNet ONNX session; EAR veto **0.18**; HTTP 400
  `LIVENESS_FAILED` / 403 `ANTISPOOF_BLOCKED` (verified previously; code paths present). ✅
- Cosine-distance equation + quoted normalization/clip snippet matches
  `pgvector_embedding_repository` comparator semantics; face = distance `<` vs voice =
  similarity `>= 0.65` (`voice.py: VERIFY_THRESHOLD = 0.65`, `SEARCH_THRESHOLD = 0.6`). ✅
- Quality: Q = 0.4·blur + 0.3·lighting + 0.3·size; pose penalty ×0.7 when |yaw|>30° or
  |pitch|>25°; hard reject Laplacian variance < 5
  (`quality/quality_assessor.py:123,133-136,145-150`). ✅ (values; see D7 for wording)
- PKCE: quoted `verifyCodeChallenge` matches `OAuth2Service.java:464-477` near-verbatim;
  S256 mandatory for public clients / plain rejected
  (`OAuth2Controller.validateAuthorizeRequest`, RFC 8252 §8.1 enforcement on
  `!client.isConfidential()`). ✅
- Auth codes single-use Redis 10-min; confidential clients require secret regardless of
  PKCE; secret-rotation grace window (V58); userinfo rejects ID tokens. ✅
- Token buckets: Bucket4j; bounded maps `MAX_ENTRIES_PER_MAP = 10_000`; eviction sweep
  every 5 min (`@Scheduled(fixedRate = 300_000)`). ✅
- ShedLock-guarded jobs: purge 03:30; guest-invitation expiry 15 min
  (`GuestLifecycleService: fixedRate = 900000`); bucket eviction 5 min. ✅
- Refresh rotation: `family_id` (V50, RefreshToken entity), family revocation in
  `RefreshTokenFamilyRevoker` with `REQUIRES_NEW`; `Persistable<UUID>`/`isNew()`. ✅
- `AntiReplayFilter` `X-Request-Nonce`, 5-min Redis window. ✅
- WebSocket `/ws/live-analysis` (`live_analysis.py:211`) + `socket.io-client` ^4.7.0 in
  `web-app/package.json`. ✅
- eMRTD passive auth: `app/domain/services/emrtd_passive_auth.py`, `asn1crypto>=1.5.1`,
  three-step chain (DG hashes → CMS/DS signature → CSCA chain), fail-closed incl. empty
  trust store and expired certs (BIO-M2 DS/CSCA validity windows). ✅
- 29 `@RestController` classes (30 files match the substring; one is the
  `@RestControllerAdvice` `GlobalExceptionHandler`, which Appendix B itself describes
  separately — so 29 controllers + 1 advice is exactly right). ✅
- Multi-tenant isolation section: filter mechanics, Role filter widened
  `(tenant_id = :tenantId OR tenant_id IS NULL)`, Identity/IdentityEmail/Consent
  unfiltered-by-design, RLS authored-but-inert (V25 = `add_row_level_security.sql` —
  the migration number cited in ch4 is correct). ✅ (see D2 for the "eight")

### Chapter 5
- Tooling table: Testcontainers, ArchUnit, Vitest+RTL, Playwright, k6, Bandit, pip-audit,
  **gitleaks v8.21.2** (both `identity-core-api` and `web-app`
  `.github/workflows/gitleaks.yml` install v8.21.2, `fetch-depth: 0`), Dependabot weekly. ✅
- Node 22 CI (`web-app/.github/workflows/ci.yml: node-version: [22.x]`); Kotlin platform
  builds on `macos-latest`/`windows-latest` (ios-build.yml, desktop-installers.yml). ✅
- Identity CI: `mvn -B -ntp -T 2C test` + JaCoCo (pom `jacoco.version 0.8.12` + check);
  integration job `-Dtest='*IntegrationTest,*IT'` with real `redis:7-alpine`; pgvector
  image retagged as `postgres:16-alpine` (ci.yml:69-107 — exactly as described). ✅
- Bio CI five jobs; `RUN_FULL_STACK_INTEGRATION` / `RUN_PROCTORING_INTEGRATION` gates;
  bare-host baseline "647 passed / 1 skipped / 1 xfailed; 50 passed / 111 skipped" matches
  the recorded baseline in `biometric-processor/CLAUDE.md`. ✅
- Admin-merge history + gate hardening disclosed (matches repo history). ✅ (honesty)
- pytest split 683 unit / 167 integration / 22 e2e / 10 benchmarks / 6 manual = 888. ✅ exact
- Playwright 336 cases / 28 spec files in `web-app/e2e/`. ✅ exact
- Vitest 1,025 / 105 files: counted 1,022 / 105 at HEAD with a strict line-start grep —
  within counting-method tolerance. ✅
- k6: exactly six scenarios with the named files; thresholds login p95<300, refresh
  p95<200, enrollment p95<2000, verification p95<500 & p99<1000, `http_req_failed
  rate<0.01`; auth ramps to 200 VUs, stress to 1,500, spike burst — all in
  `FIVUCSAS/load-tests/scenarios/`. ✅ (and correctly labeled targets)
- Locust footnote (legacy experiment) consistent with repo. ✅
- Security table implemented-vs-intent split (no ZAP/Snyk/Trivy jobs in any workflow —
  confirmed by grep). ✅
- ISO 30107-3 harness `spoof-detector/src/metrics/iso30107.py` (apcer/bpcer/acer/eer +
  CIs); `CasiaFasdMicroBench` (`web/src/validation/CasiaFasdMicroBench.ts`);
  `HybridFusionEvaluator` threshold **0.45** (`src/fusion/hybrid_evaluator.py:47`,
  `web/src/fusion/HybridEvaluator.ts:72`); multi-class taxonomy (`STATIC_IMAGE`,
  `VIDEO_REPLAY`, `MASK_3D`, `HEAVY_MAKEUP`, `AR_FILTER`, `DEEPFAKE_INJECT` in
  `SpoofCategory`); `LivenessProver` guilty-until-proven (score-to-60) present. ✅
- 13 Python analyzers ✅ exact; named analyzers all exist as files. ✅
- "100% accuracy / ACER 0.00%" poster figure explicitly refused. ✅ (posture)
- Isolation testing levels + surefire meta-assertion (ci.yml parses reports and fails if a
  named IT did not execute). ✅

### Chapter 6
- EAR blink threshold 0.21 "in production". ✅
- Passive stack components incl. rPPG (`rppg_analyzer.py`) and conservative spoof-wins
  verdict policy (`LIVENESS_VERDICT_POLICY` default conservative). ✅
- MTCNN / FaceNet-style 512-dim / MediaPipe 478-point / pgvector ANN. ✅
- "twelve selectable authentication methods" ✅ (12 LOGIN_METHODS).
- PACE / EF.CardAccess on Android (androidApp `data/nfc/pace/` —
  `PaceKeyDerivation`, `CardAccessParser`); server `POST /nfc/mrz` +
  `POST /nfc/verify-authenticity` fail-closed; asn1crypto. ✅
- National-security disclaimers (no certification claimed, no operational deployment). ✅

### Chapter 7
- All summary claims re-verified above (migrations — see D1; IVFFlat cosine; encrypted
  ciphertext column; Redis roles; Traefik admin gating; hardened containers; BCrypt 12;
  RS256 + kid routing + amr; Yubico WebAuthn (`pom.xml` `com.yubico`); two rate-limit
  layers; refresh family reuse detection; isolation re-verified per PR; ~4,400 authored
  tests across five technologies; k6 + Bandit/pip-audit/gitleaks/Dependabot). ✅
- Limitations list: RLS inert ✅; pairwise-sub dormant (flag default OFF) ✅; server-side
  fingerprint removed → WebAuthn-only ✅; iris enum-only ✅; watchlist fail-fast stub ✅;
  single-VPS SPOF ✅; no billing ✅; integration-gate trust caveat ✅ (and still accurate —
  the lane is currently red/admin-merge per 2026-06-07 notes). **Card-model bullet: see W1.**
- "ten verification steps" pluggable — 10 handler classes under
  `application/service/verification/handler/`. ✅
- "twelve authentication methods … genuinely pluggable" — 12 login methods through the
  handler registry (10 concrete `AuthMethodHandler` impls + WebAuthn/passkey + approve-login
  step handlers). ✅

### Appendices
- **Appendix A:** every migration title V0–V83 matches the actual `V{n}__{snake}.sql`
  filename one-for-one (spot-checked all rows against the directory listing), including the
  quirky `V43__noop_reserved_v43_ships_as_V48.sql`. Missing only the post-thesis **V84**
  (see D1). Alembic "5 revisions" ✅ (0001–0005).
- **Appendix B:** 29 controllers ✅; controller table endpoint paths spot-checked correct —
  AuthController (`/register /login /login/preflight /login/begin /refresh /logout /me
  /mfa/step`), NfcController (`/enroll /verify /verify-mrz /verify-authenticity`),
  ApproveLoginController (`POST /session`, `GET /pending`, `POST /session/{id}/decide`),
  OAuth2 (`/authorize /authorize/complete /token /userinfo`), OpenID
  (`/.well-known/openid-configuration`, `/jwks.json`), `POST /{id}/rotate-secret` (V58).
  `GlobalExceptionHandler` as `@RestControllerAdvice` ✅. Bio 26 modules / "roughly 69"
  endpoints — see D11.
- **Appendix C:** service inventory (8080/8001, internal-only bio, PG17+pgvector,
  Redis 7.4, Traefik v3), subdomain list, `.env.prod` discipline — consistent with composes
  and parent docs. ✅

---

## Accuracy-posture check (explicit task requirement)

All FAR/FRR/APCER/BPCER/ACER/EER and latency/capacity figures in the thesis are labeled
**targets / controlled-test / design estimates**, never audited production results
(ch2 §2.2 O2/O3 + the closing paragraph, ch3 NFR table, ch5 §5.6 table + §5.8.3, ch6 §6.2,
ch7 §7.1/§7.2.2). The unverified "100% accuracy / ACER 0.00%" poster figure is explicitly
disavowed in ch5 §5.8.3, ch6 §6.2, and ch7. **Posture intact.** ✅
